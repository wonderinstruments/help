---
title: "Git Basics"
tags: [cli, git]
---

# Git Basics

## Staging and Committing

Use `git add` to stage files, then `git commit` to save them.

    git add file.txt
    git commit -m "Add file"

## See also

For more advanced workflows, see [Git Rebase Workflow](example.md).
